import React, { useState } from "react";
import { InfoContainer } from "../product.styled";

const Document = () => {
  return (
    <InfoContainer>
      <div></div>
    </InfoContainer>
  );
};

export default Document;
