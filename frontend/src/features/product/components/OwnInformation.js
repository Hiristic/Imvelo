import React from "react";
import { InfoContainer } from "../product.styled";

const OwnInformation = () => {
  return (
    <InfoContainer>
      <div></div>
    </InfoContainer>
  );
};

export default OwnInformation;
